# Core libraries for historical ingestor
requests>=2.25.0,<3.0.0         # For making HTTP requests to Polygon.io REST API
confluent-kafka[avro,protobuf,json-schema]>=2.0.0,<3.0.0 # Kafka client with Protobuf support (though we use google.protobuf directly for serialization here)
                                        # The extra avro,protobuf,json-schema might not be needed if only using basic producer with proto bytes.
                                        # confluent-kafka by itself is usually enough.
google-protobuf>=3.19.0,<5.0.0        # For working with Protobuf messages (serialization)
boto3>=1.20.0,<2.0.0                 # AWS SDK for Python (for Secrets Manager)

# Optional, but good for production logging and environment management
# python-json-logger # For structured JSON logging if desired
# python-dotenv      # For loading .env files during local development (not for Dockerized app usually)

# For generating protobuf code (development dependency, not strictly runtime unless dynamically generating)
# grpcio-tools       # Included if generating _pb2_grpc.py, but we only need _pb2.py for messages here.
# If grpcio-tools is listed, ensure it's for dev environments or a separate requirements-dev.txt.
# For runtime, only 'protobuf' is strictly needed for the generated _pb2.py files.

# Note: Specific versions should be tested and pinned for stability.
# The versions above are examples. Check for latest stable versions.
