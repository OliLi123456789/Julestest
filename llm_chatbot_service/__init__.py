# This file makes 'llm_chatbot_service' a Python package.

# You can optionally expose parts of the package's API here.
# For example, if the FastAPI app instance was needed by another module:
# from .main import app as llm_chatbot_api_app

# Or to make config easily accessible (though direct import is also fine):
# from .config_llm import llm_service_config
