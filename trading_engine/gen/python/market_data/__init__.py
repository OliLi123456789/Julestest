# This file makes 'market_data' a package for (simulated) protos.
# In a real setup, it might import the generated modules, e.g.:
# from .market_data_pb2 import Quote, Trade, Aggregate
# For this subtask, the mock classes are defined directly in market_data_handler.py,
# so this __init__.py primarily serves to define the package structure.
# If market_data_pb2.py was created, it would be alongside this file.
