# This file makes the 'external_data_api' directory a Python package.

# You can optionally expose parts of the package's API here for easier imports by other modules,
# for example, if this FastAPI app was to be mounted as a sub-app by another main application.
# from .main import app as external_data_api_app
